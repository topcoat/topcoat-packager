# Topcoat Packager

Packages up a `Topcoat` release by tagging all the controls and main repo with a bumped MINOR.

## Prereqs

- `Node@>=0.10.0`
- Bash shell (thus far only tested on OS X)

## Usage

To stage changes simply run `./boom`. I do not reccomend adding this executable globally. 

